"""
Legacy twin module (disabled in P6.1).

All adversarial twin logic now lives in:
    twin_analysis.py

This file is kept only as a placeholder to avoid import errors
from older scripts. Do not import anything from here.
"""

def run_adversarial_twin(*args, **kwargs):
    """
    Legacy stub. Left in place for backward compatibility.
    Does nothing and returns an empty structure.
    """
    return {"twins": []}


